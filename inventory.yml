all:
  vars:
    ansible_ssh_private_key_file: ~/.ssh/shack.pem

  children:
    ubuntu_nodes:
      vars:
        ansible_user: ubuntu
      hosts:
        UB1:
          ansible_host: 172.31.35.107

    amazon_nodes:
      vars:
        ansible_user: ec2-user
      hosts:
        L2:
          ansible_host: 172.31.33.91
